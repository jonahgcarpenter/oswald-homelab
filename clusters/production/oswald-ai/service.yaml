apiVersion: v1
kind: Service
metadata:
  name: oswald-ai-service
  namespace: oswald-ai
  labels:
    app: oswald-ai
spec:
  type: ClusterIP
  selector:
    app: oswald-ai
  ports:
    - name: http
      protocol: TCP
      port: 8080
      targetPort: 8080
